##### Run Command:

`$ go run range.go`

##### Results:

`sum: 9`
`index: 1`
`a -> apple`
`b -> banana`
`key: a`
`key: b`
`0 103`
`1 111`
___
#### Note: Map iteration order is non-deterministic in Go. The `a -> apple` / `b -> banana` and `key: a` / `key: b` pairs may appear in either order.
